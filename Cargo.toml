[package]
name = "alloy-kernel"
version = "1.0.0"
edition = "2021"

[dependencies]
libp2p = { version = "0.53", features = ["tcp", "dns", "async-std", "noise", "yamux", "gossipsub"] }
futures = "0.3"
async-std = { version = "1.12", features = ["attributes"] }
rand = "0.8"